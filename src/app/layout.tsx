import type { Metadata } from "next";
import { Inter, Playfair_Display, Outfit } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
  display: "swap",
});

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "St. Peter & St. Paul Orthodox Syrian Church, Victoria",
  description: "A congregation of the Malankara Orthodox Syrian Church in Victoria, BC. Join us for traditional Orthodox worship and community life under the Canadian Diocese.",
  keywords: ["Orthodox Church Victoria", "Syrian Orthodox Church", "Malankara Orthodox", "Victoria BC Church", "Holy Qurbana Victoria"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${playfair.variable} ${outfit.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
